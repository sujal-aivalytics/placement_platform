# Database Connection (Prisma 7)
# For Supabase: Use the DIRECT connection (port 5432) without pgbouncer
# Both DATABASE_URL and DIRECT_URL should be the same in Prisma 7
DATABASE_URL="postgresql://username:password@your-host.pooler.supabase.com:5432/postgres"
DIRECT_URL="postgresql://username:password@your-host.pooler.supabase.com:5432/postgres"

# For local PostgreSQL (both should be the same):
# DATABASE_URL="postgresql://postgres:password@localhost:5432/aivalytics"
# DIRECT_URL="postgresql://postgres:password@localhost:5432/aivalytics"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"

# NextAuth Secret - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"

# Google Gemini AI API Key (Optional - for AI feedback feature)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key-here"

# IMPORTANT NOTES FOR SUPABASE USERS:
# - Use port 5432 (direct connection), NOT 6543 (pgbouncer)
# - Do NOT add ?pgbouncer=true parameter
# - Both DATABASE_URL and DIRECT_URL should be identical
# - Find your connection string in: Supabase Dashboard > Project Settings > Database > Connection String (Direct)
